# Local development must use localhost consistently with the browser frontend.
# Do not substitute 127.0.0.1 while the frontend runs on localhost; the
# SameSite=Lax authentication cookie will be treated as cross-site and blocked.
VITE_API_BASE_URL=http://localhost:8000
